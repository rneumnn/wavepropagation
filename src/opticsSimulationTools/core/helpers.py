

class TimeReference:
    LOCAL = "local"          # only current surface -> focus
    OPL = "opl"              # accumulated OPL + final segment
    GD_ONLY = "gd"                # external relative GD + final segment
    GD_WITH_OPL= "gd_with_opl"  # accumulated OPL + GD + final segment

